try {
        setIsProcessing(true);
        const result = await detectPoseAndMeasure(baby.age_months, baby.gender as 'male' | 'female');
          ageMonths,
          baby.gender as 'male' | 'female'
        );
}